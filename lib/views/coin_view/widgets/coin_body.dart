import 'package:flutter/material.dart';

class CoinBody extends StatelessWidget {
  const CoinBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
