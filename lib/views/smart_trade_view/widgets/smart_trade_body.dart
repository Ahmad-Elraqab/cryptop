import 'package:flutter/material.dart';

class SmartTradeBody extends StatelessWidget {
  const SmartTradeBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
