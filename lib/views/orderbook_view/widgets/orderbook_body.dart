import 'package:flutter/material.dart';

class OrderbookBody extends StatelessWidget {
  const OrderbookBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
